const root=document.documentElement,button=document.querySelector("button");button.addEventListener("click",()=>{button.style.pointerEvents="none",setTimeout(()=>{button.style.pointerEvents="all"},1e3);const count=parseInt(button.innerText),newCount=count-1;console.log({newCount:newCount});const audioIndex=33-newCount,audio=new Audio(`media/audio${audioIndex}.wav`);audio.play(),button.innerText=newCount,0==newCount&&root.classList.add("is-won")});